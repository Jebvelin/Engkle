rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /properties/{document} {
      allow create: if request.auth.uid == request.resource.data.user_name;
      allow read: if request.auth != null;
      allow write: if request.auth.uid == resource.data.user_name;
      allow delete: if request.auth.uid == resource.data.user_name;
    }
  }
}
